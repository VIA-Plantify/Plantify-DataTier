{
  "name": "C# Data Dev",
  "dockerComposeFile": "../docker-compose-data.yml",
  "service": "datadev",
  "workspaceFolder": "/workspace",
  "shutdownAction": "none",
  "updateRemoteUserUID": false,
  "initializeCommand": "powershell -Command \"& { $Network = 'backend'; docker network inspect $Network *> $null; if ($LASTEXITCODE -ne 0) { Write-Host \\\"[devcontainer] Creating Docker network: $Network\\\"; docker network create --driver bridge $Network } else { Write-Host \\\"[devcontainer] Docker network already exists: $Network\\\" } }\"",
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csdevkit"
      ]
    }
  }
}