{
  "name": "C# Data Dev",
  "dockerComposeFile": "../docker-compose.dev.yml",
  "service": "datadev",
  "workspaceFolder": "/workspace",
  "shutdownAction": "none",
  "updateRemoteUserUID": false,
  "postCreateCommand": "set -e; chmod +x /workspace/.scripts/*.sh; echo '[devcontainer] setup-dev'; bash /workspace/.scripts/setup-dev.sh; echo '[devcontainer] init-db'; bash /workspace/.scripts/init-db.sh",
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csdevkit"
      ]
    }
  }
}